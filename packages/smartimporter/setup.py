from setuptools import setup, find_packages

setup(
    name="smartimporter",
    version="0.1",
    packages=find_packages(),
    author="Noah",
    description="Smart Importer: Import modules with fallback installation",
    classifiers=[
        "Programming Language :: Python :: 3",
    ],
)
